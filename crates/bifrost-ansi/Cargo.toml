[package]
name = "bifrost-ansi"
version = "0.1.0"
edition = "2021"
authors = ["Bifrost Contributors"]
description = "Bifrost ANSI Bytecode compiler, ANSI tokenization, and Heatshrink LZSS compression/decompression"

[dependencies]
thiserror.workspace = true
serde.workspace = true
log.workspace = true
bifrost-compression = { version = "0.1.0", path = "../bifrost-compression" }

# heatshrink bindings will go here (or simple pure Rust implementation or dependencies)
# heatshrink = { version = "..." }
